import React from 'react';
import './style.css'

const Spinner = () => {
    return (
        <div style={{marginTop: 80}} className="ui active large centered inline loader text-center">
        </div> 
    )
}

export default Spinner;